name: Generate manifests

on:
  push:
    branches: [main]
    paths:
      - "images/**"
      - "music/**"
      - "scripts/generate-manifest.js"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-manifest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Generate manifest.json files
        run: node scripts/generate-manifest.js

      - name: Commit manifests if changed
        run: |
          if ! git diff --quiet -- images/manifest.json music/manifest.json; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add images/manifest.json music/manifest.json
            git commit -m "chore: regenerate manifests"
            git push
          else
            echo "Manifests already up to date."
          fi
